---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: ml-training-job
  namespace: {{ ml_training_namespace }}
  labels:
    app: ml-training
    component: prophet-model
spec:
  schedule: "0 2 * * 0"  # Every Sunday at 2 AM UTC
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  concurrencyPolicy: Forbid
  jobTemplate:
    metadata:
      labels:
        app: ml-training
    spec:
      backoffLimit: 1
      activeDeadlineSeconds: 5400  # 90 minutes max
      template:
        metadata:
          labels:
            app: ml-training
        spec:
          serviceAccountName: ml-training-sa
          restartPolicy: OnFailure
          # CRITICAL: Pin to permanent workers to prevent termination during training.
          # Uses nodeAffinity (not nodeSelector) because nodeSelector matches one
          # label value, not a comma-separated list. The ml_training_node_selector
          # var holds "k3s-worker-1,k3s-worker-2" by default and is split on ','.
          affinity:
            nodeAffinity:
              requiredDuringSchedulingIgnoredDuringExecution:
                nodeSelectorTerms:
                - matchExpressions:
                  - key: k3s-worker
                    operator: In
                    values: {{ (ml_training_node_selector | default('k3s-worker-1,k3s-worker-2')).split(',') | map('trim') | map('string') | list | to_json }}
          containers:
          - name: ml-trainer
            image: {{ ml_training_image }}
            imagePullPolicy: IfNotPresent
            # The bash pipeline is base64-encoded to avoid YAML literal-block
            # whitespace issues. Encoded here with a Jinja filter; container
            # decodes + execs at runtime.
            command: [/bin/bash, -c, "echo {{ ml_training_b64_script }} | base64 -d | bash"]
            env:
            - name: AWS_REGION
              value: {{ aws_region | default("ap-southeast-1") }}
            - name: AWS_DEFAULT_REGION
              value: {{ aws_region | default("ap-southeast-1") }}
            - name: STATE_TABLE
              value: {{ state_table_name }}
            - name: WAL_TABLE
              value: {{ wal_table_name }}
            - name: S3_BUCKET
              value: {{ s3_bucket_name }}
            # Add node selector environment variable for logging
            - name: PERMANENT_WORKERS
              value: "k3s-worker-1,k3s-worker-2"
{% if aws_access_key_id is defined and aws_access_key_id %}
            envFrom:
            - secretRef:
                name: aws-credentials
{% endif %}
            resources:
              requests:
                cpu: "500m"
                memory: "1Gi"
              limits:
                cpu: "1500m"
                memory: "1500Mi"
            volumeMounts:
            - name: workspace
              mountPath: /workspace
          volumes:
          - name: workspace
            emptyDir:
              sizeLimit: "5Gi"
