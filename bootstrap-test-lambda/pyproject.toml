[project]
name = "bootstrap-test-lambda"
version = "1.0.0"
description = "Lambda function for testing K3s worker bootstrap script"
requires-python = ">=3.11"
dependencies = [
    "boto3>=1.34.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"